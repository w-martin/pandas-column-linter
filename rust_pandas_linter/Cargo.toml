[package]
name = "rust_pandas_linter"
version = "0.1.0"
edition = "2021"

[dependencies]
rustpython-parser = "0.4.0"
rustpython-ast = "0.4.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
toml = "0.8"

[dev-dependencies]
tempfile = "3.8"
